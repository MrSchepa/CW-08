8 kyu
Remove First and Last Character
function removeChar(str){
  // loquen
let newArr = str.split('')
let result = newArr.splice(1, newArr.length - 2);
  
  return result.join('');
};

8 kyu
Opposite number
function opposite(number) {
  return number *= -1;
}

Remove String Spaces
const noSpace = x => x.replace(/\s+/g, '');

